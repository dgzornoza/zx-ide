## 1. Analysis and Preparation

- [x] 1.1 Examine `projects/web-client/src/extract-tiles/` implementation for `.zxp` parsing logic.
- [x] 1.2 Identify reusable utility functions or components for `.zxp` handling.

## 2. Implementation

- [x] 2.1 Update `extract-sprites` file input to accept `.zxp` files.
- [x] 2.2 Implement/Integrate `.zxp` parsing logic into `extract-sprites` component.
- [x] 2.3 Update the processing workflow in `extract-sprites` to handle the new file type.
- [x] 2.4 Ensure error handling is implemented for invalid `.zxp` files.

## 3. Verification

- [ ] 3.1 Verify `.png` extraction still works correctly in `extract-sprites`.
- [ ] 3.2 Verify `.zxp` extraction works as expected in `extract-sprites`.
- [ ] 3.3 Test with a corrupted `.zxp` file to ensure proper error handling.
